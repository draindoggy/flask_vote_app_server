   {
     "version": 2,
     "builds": [
       {
         "src": "flask_vote_app.py", // Имя вашего файла с кодом Flask
         "use": "@vercel/python",
         "config": {
           "runtime": "python3.10" // Или другая версия Python
         }
       }
     ],
     "routes": [
       {
         "src": "/(.*)",
         "dest": "/app.py" 
       }
     ]
   }
  
